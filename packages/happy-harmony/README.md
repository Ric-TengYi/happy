# Happy HarmonyOS

Native HarmonyOS ArkTS/ArkUI MVP shell for Happy.

The `src/core` directory contains TypeScript protocol-core services that are
tested with Vitest. The `entry/src/main/ets` directory contains the DevEco
ArkTS shell that will host those services once platform adapters are added.
